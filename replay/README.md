# Trace Replay

This directory contains the scripts to run trace-replay workload. Note all scripts assume the current working directory is the project top-level directory.

## 1. Download Trace
Use `download_trace.sh` to download Twitter cache traces.

```shell
bash replay/download_trace.sh
```

## 2. Preprocess Trace

```shell
bash replay/preprocess_trace.sh
```

